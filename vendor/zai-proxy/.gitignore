# Built binaries (anchored to the repo root so they never shadow source
# directories like cmd/token-collector/)
/zai-api
/zai-bridge
/zai-proxy
/zai-proxy-e2e
/token-collector
/token-minter
/q-bless
/nosdk

# Research binaries (nosdk spike)
scripts/research/nosdk/qbless

# Bundle build output (135MB TUI binary — never commit)
/dist/

# Research data dumps (netlog / TLS captures — large, derived, never commit)
*.bin
netlog*.json
mitm_dumps/
dump_h1/

# Runtime artifacts (tokens.sqlite holds harvested device tokens — never commit)
tokens.sqlite
tokens.sqlite-wal
tokens.sqlite-shm
*.log

# Blessed Q tokens are credentials — never commit
qbless.json
qbless_*.json

# Local environment
.env

# Python tooling
__pycache__/

# Debug/research logs and large raw transcripts — never commit
history_chat_for_log.md
docs/error2.md
docs/error_toolcall_log.md
docs/log_stall.md
docs/stall_evident.md
docs/stall_error_shape_new.md
log_history.md
log_e2e.md
COMMIT_MSG_TMP

# Local account configs (API keys — never commit)
/config/

# Local build scratch (A/B binaries)
/bin/
